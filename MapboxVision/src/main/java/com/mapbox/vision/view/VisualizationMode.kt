package com.mapbox.vision.view

enum class VisualizationMode {
    CLEAR, /** Original frame. */
    SEGMENTATION, /** Original frame with segmentation as an overlay. */
    DETECTION /** Original frame with detections as an overlay. */
}
